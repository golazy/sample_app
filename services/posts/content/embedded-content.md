All templates, public files, and posts are embedded in the application binary.

That means deployment only needs the compiled executable.

<script>alert("this raw HTML is intentionally escaped")</script>

#!/usr/bin/env bash
#MISE description="List users with public recipients for development secrets"
set -euo pipefail

export GOCACHE="${GOCACHE:-$PWD/.tmp/go-cache}"
mkdir -p "$GOCACHE"

go run ./internal/cmd/secrets users "$@"

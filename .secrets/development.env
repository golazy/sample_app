# Development-only values for the sample app. Production must set real values
# through the deployment environment.
SECURE_COOKIE_KEY=sample-app-development-cookie-key-0001
EXAMPLE_API_TOKEN=sample-app-development-token

#!/usr/bin/env bash
#MISE description="Add or update USER's public age recipient for development secrets"
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
bash "$SCRIPT_DIR/_lib.sh" add-key "$@"

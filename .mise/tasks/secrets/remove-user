#!/usr/bin/env bash
#MISE description="Remove USER from the development secrets recipient list"
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
bash "$SCRIPT_DIR/_lib.sh" remove-user "$@"

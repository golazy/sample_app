#!/usr/bin/env bash
#MISE description="Create a local age key for USER and register its public recipient"
set -euo pipefail

export GOCACHE="${GOCACHE:-$PWD/.tmp/go-cache}"
mkdir -p "$GOCACHE"

go run ./internal/cmd/secrets new-key "$@"
